{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Colcon build",
            "type": "shell",
            "command": "source ~/ros2_ws/install/setup.bash && colcon build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}